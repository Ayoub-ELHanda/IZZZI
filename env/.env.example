# Common
NODE_ENV=development

# Backend
PORT=4000
DATABASE_URL=postgres://izzzi:izzzi@postgres:5432/izzzi
REDIS_URL=redis://redis:6379
JWT_SECRET=change-me-long-secret-for-development-only
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d

# Stripe (leave empty for now, fill when ready)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_MENSUEL=
STRIPE_PRICE_ANNUEL=

# Synthesis API (leave empty for now, fill when ready)
SYNTHESIS_API_KEY=

# SMTP (using Mailhog for local development)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_FROM=IZZZI <no-reply@localhost>

# URLs
APP_BASE_URL=http://localhost:3000
API_BASE_URL=http://localhost:4000

# Frontend
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_ANALYTICS_ID=
NEXTAUTH_URL=http://localhost:3000
